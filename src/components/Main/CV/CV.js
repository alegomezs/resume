import { Button } from "@mui/material";

const CV = () => {
  return (
    <section>
      <h1>Curriculum Vitae 📝</h1>
      <Button variant="contained">Download</Button>
    </section>
  );
};

export default CV;
